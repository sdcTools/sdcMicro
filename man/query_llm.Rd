% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai_access_utilities.R
\name{query_llm}
\alias{query_llm}
\title{Query an LLM (provider-agnostic)}
\usage{
query_llm(
  prompt,
  system_prompt = "You are a data anonymization assistant.",
  provider = c("openai", "anthropic", "custom"),
  model = NULL,
  api_key = NULL,
  base_url = NULL,
  temperature = 0,
  tools = NULL
)
}
\arguments{
\item{prompt}{Character string with the user message.}

\item{system_prompt}{Character string with the system message.}

\item{provider}{One of \code{"openai"}, \code{"anthropic"}, or \code{"custom"}.
\code{"custom"} uses the OpenAI-compatible format with a custom \code{base_url}.}

\item{model}{Model identifier. If \code{NULL}, defaults to \code{"gpt-4.1"} for OpenAI,
\code{"claude-sonnet-4-20250514"} for Anthropic.}

\item{api_key}{API key. If \code{NULL}, auto-detected from environment variables:
\code{OPENAI_API_KEY}, \code{ANTHROPIC_API_KEY}, or \code{LLM_API_KEY}.}

\item{base_url}{Base URL for the API. Defaults per provider; required for \code{"custom"}.}

\item{temperature}{Sampling temperature (default 0 for deterministic output).}

\item{tools}{Optional list of tool schemas (from \code{get_tool_schemas()}). When provided,
enables tool calling and returns a structured list instead of a string.}
}
\value{
When \code{tools = NULL}, the LLM response content as a character string.
  When \code{tools} is provided, a list with \code{$tool_calls} (list of normalized
  tool call lists, each with \code{$tool} and parameters) and \code{$content}
  (text content or NULL).
}
\description{
Sends a prompt to an LLM and returns the response content as a string.
Supports OpenAI, Anthropic, and any OpenAI-compatible endpoint (Ollama, Azure, vLLM, Groq, Together, etc.).
When \code{tools} is provided, uses native tool calling (OpenAI/Anthropic) or JSON fallback (custom).
}
\keyword{internal}
